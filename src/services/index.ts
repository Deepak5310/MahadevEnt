// src/services/index.ts
// Barrel export for all service-layer modules.
// This is the ONLY layer that communicates with external APIs or storage.
// Example: export { apiClient } from './apiClient'
//          export { authService } from './authService'
//          export * as mockData from './mockData'
